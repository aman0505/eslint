import finq from "./test"
const fun=(x)=>{
    if(x>30){
        console.log(window.location.href)
        
        return true;
        // this is main files  comments 
        // main1
        // main 3
    }
    finq();
    return true;
}
const x=50;
console.log(fun(x))
